[databases]
* = host=127.0.0.1 auth_user=pgbouncer

[pgbouncer]
user = postgres
listen_addr = *
listen_port = 6432
admin_users = pgbouncer
pool_mode = session
auth_type = hba
auth_hba_file = /etc/pgbouncer/pg_hba.conf
auth_query = SELECT * FROM public.user_lookup($1);