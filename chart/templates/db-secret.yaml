kind: Secret
apiVersion: v1
metadata:
  name: db-passwords
stringData:
  POSTGRES_PASSWORD: {{ .Values.db.postgres.users.su.password }}
  REPLICATION_USER_PASSWORD: {{ .Values.db.postgres.users.replication.password }}